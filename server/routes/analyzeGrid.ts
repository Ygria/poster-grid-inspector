import { Router } from 'express'; import { analyzePoster } from '../ai/analyzePoster.js'; import { AiError } from '../ai/errors.js';
export const analyzeGridRouter=Router();
analyzeGridRouter.post('/api/analyze-grid',async(req,res)=>{const {imageBase64,mimeType='image/jpeg'}=req.body||{};if(typeof imageBase64!=='string'||!imageBase64)return res.status(400).json({success:false,code:'INVALID_IMAGE',error:'请选择有效的海报图片。',retryable:false});try{const out=await analyzePoster(imageBase64,mimeType);return res.json({success:true,...out});}catch(e:any){const err=e instanceof AiError?e:new AiError('INTERNAL_ERROR','海报分析发生未知错误。',500,false,e);console.error('[poster-analysis]',{code:err.code,message:err.message});return res.status(err.status).json({success:false,code:err.code,error:err.message,retryable:err.retryable});}});
